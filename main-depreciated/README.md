# Legacy Archive

This directory contains the archived research workspace that originally lived at the repository root.

- `legacy-workspace`: old code, notebooks, exploratory scripts, and checkpoint files
- `legacy-results`: old root-level readme and result dumps
- `legacy-figures`: archived plots and figures

The supported package now lives in `src/discreteNPIV`.

